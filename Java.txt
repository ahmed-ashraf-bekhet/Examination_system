1-Which of the following is true about inheritance in Java?
1) Private methods are final.
2) Protected members are accessible within a package and 
   inherited classes outside the package.
3) Protected methods are final.
4) We cannot override private methods

1) 1, 2 and 4 //write Answer
2) Only 1 and 2
3) 1, 2 and 3
4) 2, 3 and 4



2-Which of the following is true about inheritance in Java. 
1) In Java all classes inherit from the Object class directly or indirectly. 
The Object class is root of all classes. 
2) Multiple inheritance is not allowed in Java. 
3) Unlike C++, there is nothing like type of inheritance in Java where we can specify whether the inheritance is protected, public or private.

1) 1, 2 and 4 //write Answer
2) 1 and 2
3) 2 and 3
4) 1 and 3

3-Which of the following is/are advantages of packages?
1) Packages avoid name clashes
2) Classes, even though they are visible outside their package, can have fields visible to packages only
3) We can have hidden classes that are used by the packages, but not visible outside.
4) All of the above //Write Answer



4-Which one of the following is correct?

1) Java applets can not be written in any programming language
2) An applet is not a small program
3) An applet can be run on its own
4) Applets are embedded in another applications //write Answer


5-Which of the following is true about interfaces in java.

1) An interface can contain following type of members.
....public, static, final fields (i.e., constants) 
....default and static methods with bodies

2) An instance of interface can be created.

3) A class can implement multiple interfaces.

4) Many classes can implement the same interface.

1) 1, 3 and 4 //write Answer
2) 1, 2 and 4
3) 2, 3 and 4
4) 1, 2, 3 and 4



6-Type IV JDBC driver is a driver
1) which is written in C++
2) which requires an intermediate layer
3) which communicates through Java sockets //write Ans
4) which translates JDBC function calls into API not native to DBMS


7-Which of the following is used to make an Abstract class?
1) Making atleast one member function as pure virtual function //write Ans
2) Making atleast one member function as virtual function
3) Declaring as Abstract class using virtual keyword
4) Declaring as Abstract class using static keyword



8-A Java Servlet is a server-side component that runs on the web server and extends the capabilities of a server.
1) True // Write Answer
2) False

9-A class can inherit from multiple abstract classes.
1) True 
2) False // Write Answer

10-An instance of interface can be created.
1) True 
2) False // Write Answer

11-An abstract class is one that is not used to create objects
1) True // Write Answer
2) False

12-An abstract class is designed only to act as a base class to be inherited by other classes.
1) True // Write Answer
2) False

13-Java uses threads to enable the entire environment to be Synchronous
1) True 
2) False // Write Answer


14-In Java, when we implement an interface method, it must be declared as Protected
1) True 
2) False // Write Answer